let num = 1; // 숫자 (Number)
console.log(num);

let text = "Hello"; // 문자열 (String)
console.log(text);
let message = "안녕하세요"; // 작은 따옴표 혹은 큰 따옴표로 선언
console.log(message);

let good = true; // 참, 거짓 (Boolean)
console.log(good);
let bad = false;
console.log(bad);

let none = null; // null, 값이 없음을 의미함
console.log(none);
let value; // undefined, 값이 아직 정해지지 않음
console.log(value);
